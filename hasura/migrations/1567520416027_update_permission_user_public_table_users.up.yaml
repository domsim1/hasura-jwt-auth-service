- args:
    permission:
      allow_aggregations: false
      columns:
      - created_at
      - email
      - first_name
      - id
      - last_name
      - password
      filter:
        id:
          _eq: X-Hasura-User-Id
      limit: null
    role: user
    table:
      name: users
      schema: public
  type: create_select_permission
